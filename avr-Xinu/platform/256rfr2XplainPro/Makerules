# This Makefile contains rules to build files in the platform/246rfr2XplainPro directory.

# Name of this component (the directory this file is stored in)
COMP = platform/256rfr2XplainPro

# Source files for this component

# Base files for this target
C_FILES = clkinit.c clkint.c create.c initialize.c kprintf.c

# Add the files to the compile source path
DIR = ${TOPDIR}/${COMP}
COMP_SRC += ${C_FILES:%=${DIR}/%}
